# Buildings

Put each building model in its own folder:

```text
assets/buildings/house/model.glb
assets/buildings/house/config.json
```

The same convention will be used for farms, towers, churches and other structures.